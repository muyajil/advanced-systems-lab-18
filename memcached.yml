# Docker compose file to run memcached servers locally

memcached_1:
  image: memcached
  net: "host"
  ports:
    - "11211:11211"

memcached_2:
  image: memcached
  net: "host"
  ports:
    - "11212:11212"

memcached_3:
  image: memcached
  net: "host"
  ports:
    - "11213:11213"
